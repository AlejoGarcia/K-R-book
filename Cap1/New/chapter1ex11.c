/*
 * I would test it with a variaiety of inputs and count each letter to check if it keeps count properly. The same with words and lines.
 * Would also put several tabs and spaces between words to check if it parses them correctly.
 */
